#include <stdio.h>

int process_double_integer_arrays(int* int_arr, int int_arr_size,
				  double* double_arr, int double_arr_size)
{
  int int_sum = 0;
  printf("Received integer array of size %d\n", int_arr_size);
  for (int i = 0; i < int_arr_size; i++)
    {
      int_sum += int_arr[i];
      // You can modify the array in C, and changes will be
      // reflected in C#
     int_arr[i] = int_arr[i] * 2; 
    }

    printf("Received double array of size %d\n", double_arr_size);
    for (int i = 0; i < double_arr_size; i++)
    {
        // Example: modify the array in-place
        double_arr[i] = double_arr[i] * 2.0; 
    }

    return sum;
}
